#!/bin/sh

# KNULLI-KR: ROCKNIX-style boot MOTD visual.
# Do not start fbv/mpv here; draw the text screen directly to /dev/fb0.

case "$1" in
    start)
        [ -x /usr/bin/knulli-console-motd ] && /usr/bin/knulli-console-motd &
        ;;
    stop)
        ;;
    *)
        ;;
esac

exit 0
